# UAVSimPlatform 项目分析报告

生成日期: 2025-12-04

## 目录
1. [项目概览](#项目概览)
2. [技术栈](#技术栈)
3. [架构设计](#架构设计)
4. [功能点分析](#功能点分析)
5. [潜在问题](#潜在问题)
6. [优化建议](#优化建议)
7. [总结](#总结)

---

## 项目概览

**UAVSimPlatform** 是一个基于 Qt 框架开发的无人机（UAV）仿真平台，主要用于可视化展示多架无人机的飞行路径和实时状态。项目采用 C++17 标准，使用 CMake 构建系统。

### 项目结构
```
UAVSimPlatform/
├── Service/           # 业务逻辑层（数据模型、仿真管理、航迹生成）
│   ├── UavModel.h/cpp
│   ├── SimulationManager.h/cpp
│   ├── TrajectoryGenerator.h/cpp
│   └── FlightPath.h/cpp
├── Gui/              # 界面展示层（主窗口、图形视图、图元）
│   ├── MainWindow.h/cpp
│   ├── SimScene.h/cpp
│   ├── SimView.h/cpp
│   ├── UavItem.h/cpp
│   ├── PathItem.h/cpp
│   ├── UavLabelItem.h/cpp
│   └── main.cpp
└── build/            # 构建输出目录
```

---

## 技术栈

| 组件 | 版本/技术 |
|------|----------|
| 编程语言 | C++17 |
| GUI框架 | Qt5 (Widgets, Core, Concurrent, Sql) |
| 构建系统 | CMake 3.16+ |
| 图形渲染 | QGraphicsView/QGraphicsScene |
| 内存管理 | std::unique_ptr (部分使用) |

---

## 架构设计

### 分层架构

#### 1. Service 层（业务逻辑层）
负责核心业务逻辑，与界面解耦。编译为静态库 `libService.a`。

**主要组件：**
- **UavModel**: 无人机数据模型
- **SimulationManager**: 仿真管理器
- **TrajectoryGenerator**: 航迹生成工具类
- **FlightPath**: 飞行路径类（目前为空实现）

#### 2. Gui 层（界面展示层）
负责用户界面展示和交互，依赖 Service 层。编译为可执行文件 `UavSimApp`。

**主要组件：**
- **MainWindow**: 主窗口控制器
- **SimScene**: 自定义场景（网格背景、事件处理）
- **SimView**: 自定义视图（缩放、平移）
- **UavItem**: 无人机图元
- **PathItem**: 航迹图元
- **UavLabelItem**: 可拖动标签图元

### 数据流设计
```
[SimulationManager]
    └─> 创建 [UavModel] 集合
         └─> 使用 [TrajectoryGenerator] 生成航迹
              └─> [MainWindow] 读取数据并创建图元
                   └─> [SimScene/SimView] 渲染显示
                        └─> 定时器驱动更新循环
```

---

## 功能点分析

### 核心功能

#### 1. 多无人机仿真管理
**位置**: `Service/simulationmanager.cpp:14-55`

**功能描述**:
- 创建多架无人机（默认5架）
- 使用网格布局分散无人机航迹，避免重叠
- 为不同无人机分配不同类型的航迹（圆形、8字形、航点连接）

**实现细节**:
```cpp
// 网格布局算法
const int cols = static_cast<int>(std::ceil(std::sqrt(total)));
const int rows = (total + cols - 1) / cols;
const qreal spacing = 300.0;  // 网格间距
```

**优点**:
- 使用智能指针 `std::unique_ptr` 管理无人机对象，避免内存泄漏
- 网格布局避免了航迹视觉上的重叠

**缺点**:
- 无人机数量硬编码为5
- 航迹类型分配逻辑硬编码

---

#### 2. 航迹生成算法
**位置**: `Service/trajectorygenerator.cpp`

提供三种航迹生成方式：

##### 2.1 圆形航迹
**函数**: `createCirclePath(center, radius, pointsCount)`

**数学原理**:
```cpp
x = center_x + radius * cos(θ)
y = center_y + radius * sin(θ)
```

**特点**:
- 参数化生成，点数可配置
- 预分配内存优化性能

##### 2.2 八字形航迹
**函数**: `createEightShapePath(center, radius, pointsCount)`

**数学原理**: 伯努利双纽线（Lemniscate of Bernoulli）
```cpp
x = r * cos(t)
y = r * sin(t) * cos(t)
```

##### 2.3 航点连接航迹
**函数**: `createPathFromWaypoints(waypoints, stepSize)`

**实现方式**:
- 计算航点间的距离和方向向量
- 使用 `QVector2D` 进行线性插值
- 按固定步长生成中间点

**优点**:
- 支持自定义航点
- 插值密度可配置

---

#### 3. 实时可视化系统

##### 3.1 图形场景管理
**位置**: `Gui/simscene.cpp`

**功能**:
- 动态绘制网格背景（50px网格）
- 绘制坐标原点标记
- 只绘制可见区域（性能优化）

**实现亮点**:
```cpp
// 计算可见区域的网格起点
qreal startX = std::floor(left / gridSize) * gridSize;
qreal startY = std::floor(top / gridSize) * gridSize;
```

##### 3.2 交互式视图
**位置**: `Gui/simview.cpp`

**功能**:
- 鼠标滚轮缩放（1.1x / 0.9x）
- 鼠标拖拽平移
- 以鼠标位置为锚点缩放

##### 3.3 图元系统
| 图元类型 | 文件 | 功能 | Z-Index |
|---------|------|------|---------|
| PathItem | pathitem.cpp | 绘制红色虚线航迹和航迹点 | -1（底层） |
| UavItem | uavitem.cpp | 绘制蓝色圆点和方向指示线 | 1（上层） |
| UavLabelItem | uavlabelitem.cpp | 可拖动标签，虚线连接到UAV | 默认 |

---

#### 4. 遥测数据展示
**位置**: `Gui/MainWindow.cpp:186-235`

**功能**:
- QTableWidget 展示多架无人机状态
- 实时更新位置坐标（保留2位小数）
- 点击无人机时高亮对应表格行
- 支持行选择和滚动定位

**数据结构**:
```cpp
QMap<int, int> m_uavRowMap;  // UAV ID -> 表格行号映射
```

---

#### 5. 仿真控制系统
**位置**: `Gui/MainWindow.cpp:32-77`

**功能**:
- QTimer 驱动仿真循环（1000ms间隔）
- 暂停/继续控制
- 自动循环航迹（到达终点后重置）

**更新逻辑**:
```cpp
void updatePathTimeout() {
    for (const auto &uav : m_simManager->getUavs()) {
        uav->updatePosition(m_currentStep);
        // 更新图元位置
        // 更新标签位置
        // 更新遥测数据
    }
}
```

---

#### 6. 点击交互系统
**位置**: `Gui/simscene.cpp:44-64`, `Gui/MainWindow.cpp:237-277`

**功能流程**:
1. 用户点击场景中的 UavItem 或 UavLabelItem
2. SimScene 发射 `uavClicked(uavId)` 信号
3. MainWindow 响应信号：
   - 高亮表格中对应行
   - 显示该UAV标签的详细信息
   - 隐藏其他UAV标签的信息

**技术实现**:
- 使用 `dynamic_cast` 识别图元类型
- Qt 信号槽机制实现解耦

---

## 潜在问题

### 严重问题（必须修复）

#### 问题1: 内存泄漏风险 🔴
**位置**: `Gui/MainWindow.cpp:18, 25-30`

**问题描述**:
```cpp
m_simManager = new SimulationManager();  // Line 18

MainWindow::~MainWindow() {
    // 没有 delete m_simManager;
    // 没有 delete updatePathTimer;
}
```

**影响**: 每次启动/关闭应用都会泄漏内存。

**修复方案**:
```cpp
// 方案1: 在析构函数中手动释放
~MainWindow() {
    delete m_simManager;
    delete updatePathTimer;
}

// 方案2: 使用智能指针（推荐）
std::unique_ptr<SimulationManager> m_simManager;
std::unique_ptr<QTimer> updatePathTimer;
```

---

#### 问题2: 严重的步进逻辑错误 🔴
**位置**: `Gui/MainWindow.cpp:32-63`

**问题描述**:
```cpp
void MainWindow::updatePathTimeout() {
    for (const auto &uav : m_simManager->getUavs()) {
        uav->updatePosition(m_currentStep);  // 所有UAV使用相同的步进值
        m_currentStep++;                     // ❌ 在循环内递增！
        // ...
    }
}
```

**问题分析**:
- 假设有3架UAV，循环执行时：
  - UAV1: step=0, 然后 step++ -> 1
  - UAV2: step=1, 然后 step++ -> 2
  - UAV3: step=2, 然后 step++ -> 3
- 导致每架UAV的位置不同步
- 每次定时器触发，步进值增加的数量 = UAV数量

**正确实现**:
```cpp
// 方案1: 循环外递增（所有UAV同步移动）
void MainWindow::updatePathTimeout() {
    for (const auto &uav : m_simManager->getUavs()) {
        uav->updatePosition(m_currentStep);
        // 更新图元和遥测...
    }
    m_currentStep++;  // 移到循环外
}

// 方案2: 每个UAV独立步进（支持不同速度）
class UavModel {
private:
    int m_currentStep = 0;  // 每个UAV独立的步进计数
};
```

---

#### 问题3: 空实现的FlightPath类 🟡
**位置**: `Service/flightpath.h/cpp`

**问题描述**:
```cpp
class FlightPath {
public:
    FlightPath();
private:
    QList<QPointF> m_path;  // 未使用
};
```

**影响**:
- 增加编译时间和二进制大小
- 代码库中存在"死代码"，降低可维护性

**建议**:
- 如果计划实现，应添加 TODO 注释
- 如果不需要，从项目中移除

---

### 中等问题（建议修复）

#### 问题4: 性能问题 🟡
**位置**: `Gui/MainWindow.cpp:58, 149`

**问题4.1: 高频调试输出**
```cpp
qDebug() << "xujunwei:" << uav.get()->getId() << "," << m_currentStep << endl;
```
- 每个UAV每秒输出一次
- 5个UAV = 5次/秒
- 100个UAV = 100次/秒（严重影响性能）

**问题4.2: 为每个UAV创建独立PathItem**
```cpp
for (const auto &uav : m_simManager->getUavs()) {
    m_pathItem = new PathItem(uav->getPath());  // 每个UAV一个PathItem
    m_scene->addItem(m_pathItem);
}
```
- 大量UAV时导致大量图元
- 重复使用同一个指针变量 `m_pathItem`（只保留了最后一个）

**修复方案**:
```cpp
// 4.1: 使用条件编译
#ifdef QT_DEBUG
    qDebug() << "UAV" << uav.get()->getId() << "at step" << m_currentStep;
#endif

// 4.2: 为每个PathItem使用独立指针
QMap<int, PathItem*> m_pathItemMap;
for (const auto &uav : m_simManager->getUavs()) {
    auto *pathItem = new PathItem(uav->getPath());
    m_pathItemMap[uav->getId()] = pathItem;
    m_scene->addItem(pathItem);
}
```

---

#### 问题5: 硬编码配置 🟡
**位置**: 多处

| 硬编码项 | 位置 | 当前值 | 建议 |
|---------|------|--------|------|
| UAV数量 | SimulationManager.cpp:17 | 5 | 从配置文件读取 |
| 定时器间隔 | MainWindow.cpp:22,74 | 1000ms | 可配置的仿真速度 |
| 网格间距 | SimulationManager.cpp:20 | 300.0 | 可配置的布局参数 |
| 航点坐标 | SimulationManager.cpp:40-42 | (122,69), (602,619) | 从文件加载 |

**建议方案**:
```cpp
// 使用 QSettings 或 JSON 配置文件
class SimulationConfig {
public:
    int uavCount = 5;
    int timerInterval = 1000;
    qreal gridSpacing = 300.0;
    QVector<QPointF> waypoints;

    void loadFromFile(const QString &path);
};
```

---

#### 问题6: 架构耦合 🟡
**位置**: `Gui/MainWindow.cpp`

**问题描述**:
- MainWindow 直接操作 `UavModel` 的内部状态
- Service层没有继承 `QObject`，无法使用信号槽
- 违反了"界面层不应直接修改业务层"的原则

**示例**:
```cpp
// MainWindow直接调用UavModel方法
uav->updatePosition(m_currentStep);
uav->setCurrentPoint(...);
```

**建议改进**:
```cpp
// UavModel 继承 QObject 并发射信号
class UavModel : public QObject {
    Q_OBJECT
signals:
    void positionChanged(int id, double x, double y);
    void pathChanged(int id, const QVector<QPointF> &path);
};

// SimulationManager 提供控制接口
class SimulationManager : public QObject {
    Q_OBJECT
public slots:
    void start();
    void pause();
    void step();  // 单步执行
signals:
    void uavUpdated(int id, double x, double y);
};

// MainWindow 只监听信号，不直接调用
connect(m_simManager, &SimulationManager::uavUpdated,
        this, &MainWindow::onUavPositionUpdated);
```

---

### 轻微问题（可选修复）

#### 问题7: 代码规范 🔵
1. **中英文混用**: 注释用中文，代码用英文
2. **注释掉的代码**: MainWindow.cpp:14, 127-131, 141-144
3. **调试标识**: "xujunwei" 出现在代码中
4. **不一致的头文件保护**: 同时使用 `#pragma once` 和 `#ifndef`

---

#### 问题8: 未使用的依赖 🔵
**位置**: `CMakeLists.txt:21-26`

```cmake
find_package(Qt5 REQUIRED COMPONENTS
    Core
    Widgets
    Concurrent  # ❌ 未使用
    Sql         # ❌ 未使用
)
```

**影响**: 增加构建时间和依赖复杂度

---

#### 问题9: 边界条件处理不当 🔵
**位置**: `Gui/MainWindow.cpp:38-40`

```cpp
if (m_currentStep >= std::numeric_limits<int>::max()) {
    m_currentStep = 0;
}
```

**问题**: 使用 `INT_MAX` 作为边界，但实际只需要航迹长度。

**改进**:
```cpp
int pathSize = uav->getPath().size();
if (m_currentStep >= pathSize) {
    m_currentStep = 0;
}
```

---

## 优化建议

### 高优先级优化（立即实施）

#### 1. 修复内存泄漏
**实施难度**: 低
**影响范围**: MainWindow

**步骤**:
1. 将裸指针改为智能指针
2. 或在析构函数中添加 delete

**参考代码**:
```cpp
// MainWindow.h
class MainWindow : public QMainWindow {
private:
    std::unique_ptr<SimulationManager> m_simManager;
    std::unique_ptr<QTimer> updatePathTimer;
};

// MainWindow.cpp
MainWindow::MainWindow(QWidget *parent) {
    updatePathTimer = std::make_unique<QTimer>(this);
    m_simManager = std::make_unique<SimulationManager>();
}
```

---

#### 2. 修复步进逻辑错误
**实施难度**: 低
**影响范围**: MainWindow::updatePathTimeout()

**方案A（简单）**: 移动递增到循环外
```cpp
void MainWindow::updatePathTimeout() {
    for (const auto &uav : m_simManager->getUavs()) {
        uav->updatePosition(m_currentStep);
        // ... 更新图元 ...
    }
    m_currentStep++;  // 移到这里
    if (m_currentStep >= maxSteps) {
        m_currentStep = 0;
    }
}
```

**方案B（灵活）**: 每个UAV独立步进
```cpp
// UavModel.h
class UavModel {
private:
    int m_currentStep = 0;
public:
    void stepForward() {
        m_currentStep = (m_currentStep + 1) % m_path.size();
        updatePosition(m_currentStep);
    }
};

// MainWindow.cpp
void MainWindow::updatePathTimeout() {
    for (const auto &uav : m_simManager->getUavs()) {
        uav->stepForward();  // 每个UAV独立步进
        // ... 更新图元 ...
    }
}
```

---

#### 3. 移除调试代码和无用注释
**实施难度**: 低
**影响范围**: 全项目

**清理内容**:
- 删除所有 `qDebug()` 调试输出（或条件编译）
- 删除注释掉的旧代码
- 移除个人标识 "xujunwei"

---

#### 4. 处理FlightPath类
**实施难度**: 低

**方案1**: 移除（如果不需要）
```bash
# 从项目中移除
rm Service/flightpath.h Service/flightpath.cpp
# 更新 Service/CMakeLists.txt
```

**方案2**: 实现（如果需要）
```cpp
class FlightPath {
public:
    void addWaypoint(const QPointF &point);
    void smooth();  // 平滑航迹
    void validate(); // 验证航迹合法性
    QVector<QPointF> getPath() const;
};
```

---

### 中优先级优化（计划实施）

#### 5. 架构解耦（引入信号槽机制）
**实施难度**: 中
**影响范围**: Service层和Gui层

**重构步骤**:

**Step 1**: UavModel 继承 QObject
```cpp
// Service/UavModel.h
class UavModel : public QObject {
    Q_OBJECT
public:
    UavModel(int id, QString name, QObject *parent = nullptr);

signals:
    void positionChanged(int id, double x, double y);
    void pathUpdated(int id);

public slots:
    void stepForward();
    void setFlightPath(const QVector<QPointF> &path);
};
```

**Step 2**: SimulationManager 提供控制接口
```cpp
class SimulationManager : public QObject {
    Q_OBJECT
public:
    SimulationManager(QObject *parent = nullptr);

public slots:
    void start();
    void pause();
    void step();
    void setSpeed(int interval);

signals:
    void simulationStarted();
    void simulationPaused();
    void allUavsUpdated();

private:
    QTimer *m_timer;
    std::vector<std::unique_ptr<UavModel>> m_uavs;
};
```

**Step 3**: MainWindow 只响应信号
```cpp
class MainWindow : public QMainWindow {
private slots:
    void onUavPositionChanged(int id, double x, double y);
    void onSimulationStateChanged();

private:
    void connectSignals();
};

void MainWindow::connectSignals() {
    for (const auto &uav : m_simManager->getUavs()) {
        connect(uav.get(), &UavModel::positionChanged,
                this, &MainWindow::onUavPositionChanged);
    }
}
```

---

#### 6. 配置文件系统
**实施难度**: 中
**建议格式**: JSON

**示例配置文件** (`config.json`):
```json
{
    "simulation": {
        "uav_count": 10,
        "timer_interval": 500,
        "auto_loop": true
    },
    "layout": {
        "grid_spacing": 400.0,
        "scene_size": 40000
    },
    "trajectories": {
        "default_type": "circle",
        "circle_radius": 50.0,
        "points_count": 60,
        "custom_waypoints": [
            [122, 69],
            [602, 619],
            [800, 300]
        ]
    }
}
```

**加载代码**:
```cpp
#include <QJsonDocument>
#include <QJsonObject>
#include <QFile>

class Config {
public:
    static Config& instance();
    void load(const QString &path);

    int uavCount() const { return m_uavCount; }
    int timerInterval() const { return m_timerInterval; }
    // ... 其他getter ...

private:
    int m_uavCount = 5;
    int m_timerInterval = 1000;
    qreal m_gridSpacing = 300.0;
};
```

---

#### 7. 性能优化
**实施难度**: 中到高

**优化7.1: 批量更新图元**
```cpp
// 使用 QGraphicsScene::itemsBoundingRect() 判断是否需要更新
void MainWindow::updatePathTimeout() {
    m_scene->setUpdatesEnabled(false);  // 暂停渲染

    for (const auto &uav : m_simManager->getUavs()) {
        uav->stepForward();
        updateUavGraphics(uav.get());
    }

    m_scene->setUpdatesEnabled(true);   // 批量渲染
    m_scene->update();
}
```

**优化7.2: 视口剔除**
```cpp
void MainWindow::updatePathTimeout() {
    QRectF visibleRect = m_view->mapToScene(m_view->viewport()->rect()).boundingRect();

    for (const auto &uav : m_simManager->getUavs()) {
        QPointF pos(uav->getX(), uav->getY());
        if (visibleRect.contains(pos)) {
            // 只更新可见区域的UAV
            updateUavGraphics(uav.get());
        }
    }
}
```

**优化7.3: 使用 QGraphicsItemGroup**
```cpp
// 将相关图元组合在一起
auto *uavGroup = new QGraphicsItemGroup();
uavGroup->addToGroup(uavItem);
uavGroup->addToGroup(labelItem);
m_scene->addItem(uavGroup);
```

---

### 低优先级优化（长期改进）

#### 8. 代码规范和文档
**建议工具**:
- Clang-Format: 代码格式化
- Doxygen: 生成文档
- Clang-Tidy: 静态分析

**示例 Doxygen 注释**:
```cpp
/**
 * @brief 生成圆形航迹
 * @param center 圆心坐标
 * @param radius 半径（单位：像素）
 * @param pointsCount 生成的点数（越多越平滑）
 * @return 航迹点集合
 *
 * 使用三角函数生成均匀分布在圆周上的点。
 * 时间复杂度：O(n)，空间复杂度：O(n)
 */
static QVector<QPointF> createCirclePath(
    QPointF center,
    double radius,
    int pointsCount
);
```

---

#### 9. 功能增强建议

##### 9.1 碰撞检测
```cpp
class CollisionDetector {
public:
    bool checkCollision(const QVector<UavModel*> &uavs, double safeDistance);
    QVector<QPair<int, int>> getCollisionPairs();
};
```

##### 9.2 航迹编辑
- 支持鼠标点击添加航点
- 支持拖拽修改航点位置
- 支持删除航点

##### 9.3 仿真速度控制
```cpp
// 添加速度控制滑块
QSlider *speedSlider = new QSlider(Qt::Horizontal);
speedSlider->setRange(1, 10);  // 0.1x ~ 10x速度
connect(speedSlider, &QSlider::valueChanged, [this](int value) {
    int interval = 1000 / value;
    updatePathTimer->setInterval(interval);
});
```

##### 9.4 录制/回放功能
```cpp
class SimulationRecorder {
public:
    void startRecording();
    void stopRecording();
    void saveToFile(const QString &path);
    void loadFromFile(const QString &path);
    void replay();
};
```

##### 9.5 更多航迹算法
- Bezier曲线
- B-Spline曲线
- Dubins路径
- RRT（快速随机树）

---

#### 10. 测试体系建设
**建议框架**: Qt Test

**示例单元测试**:
```cpp
#include <QtTest/QtTest>

class TestTrajectoryGenerator : public QObject {
    Q_OBJECT
private slots:
    void testCirclePath() {
        QPointF center(0, 0);
        auto path = TrajectoryGenerator::createCirclePath(center, 50, 60);

        QCOMPARE(path.size(), 60);  // 检查点数
        QVERIFY(qAbs(path[0].x() - 50.0) < 0.01);  // 检查第一个点
    }

    void testEightShapePath() {
        // ...
    }
};

QTEST_MAIN(TestTrajectoryGenerator)
#include "test_trajectory.moc"
```

**运行测试**:
```bash
cd build
ctest --output-on-failure
```

---

## 总结

### 项目优点
1. **清晰的分层架构**: Service和Gui分离，便于维护
2. **使用智能指针**: 部分代码使用了 `std::unique_ptr`，避免内存泄漏
3. **优秀的可视化效果**: 网格背景、航迹、标签等设计合理
4. **良好的交互性**: 支持缩放、平移、点击选中等操作
5. **代码注释详细**: 有大量教学性质的注释

### 主要缺陷
1. **内存管理问题**: 存在内存泄漏风险
2. **严重的逻辑错误**: 步进计数器在循环内递增
3. **硬编码过多**: 配置项应该外部化
4. **性能优化不足**: 存在不必要的高频输出和图元创建

### 改进优先级
```
高优先级（立即修复）:
  ├─ 修复内存泄漏（智能指针或手动delete）
  ├─ 修复步进逻辑错误
  ├─ 移除调试代码
  └─ 处理FlightPath空实现

中优先级（计划实施）:
  ├─ 架构解耦（引入信号槽）
  ├─ 配置文件系统
  └─ 性能优化（批量更新、视口剔除）

低优先级（长期改进）:
  ├─ 代码规范和文档
  ├─ 功能增强
  └─ 测试体系
```

### 推荐的下一步行动
1. **立即修复**严重问题1和2（预计1-2小时）
2. **重构**步进逻辑，使每个UAV独立管理自己的状态
3. **引入配置文件**，消除硬编码（预计2-3小时）
4. **重构架构**，实现Service层的信号槽机制（预计1天）
5. **添加单元测试**，确保核心功能正确性（预计1天）

---

## 附录

### A. 文件清单
| 文件路径 | 行数 | 功能描述 |
|---------|------|---------|
| CMakeLists.txt | 31 | 项目构建配置 |
| Service/UavModel.h | 35 | 无人机数据模型头文件 |
| Service/UavModel.cpp | 69 | 无人机数据模型实现 |
| Service/SimulationManager.h | 21 | 仿真管理器头文件 |
| Service/SimulationManager.cpp | 56 | 仿真管理器实现 |
| Service/TrajectoryGenerator.h | 38 | 航迹生成器头文件 |
| Service/TrajectoryGenerator.cpp | 82 | 航迹生成器实现 |
| Service/FlightPath.h | 18 | 飞行路径头文件（空） |
| Service/FlightPath.cpp | 7 | 飞行路径实现（空） |
| Gui/MainWindow.h | 62 | 主窗口头文件 |
| Gui/MainWindow.cpp | 278 | 主窗口实现 |
| Gui/SimScene.h | 28 | 自定义场景头文件 |
| Gui/SimScene.cpp | 65 | 自定义场景实现 |
| Gui/SimView.h | 19 | 自定义视图头文件 |
| Gui/SimView.cpp | 21 | 自定义视图实现 |
| Gui/UavItem.h | 29 | 无人机图元头文件 |
| Gui/UavItem.cpp | 29 | 无人机图元实现 |
| Gui/PathItem.h | 24 | 航迹图元头文件 |
| Gui/PathItem.cpp | 51 | 航迹图元实现 |
| Gui/UavLabelItem.h | 53 | 标签图元头文件 |
| Gui/UavLabelItem.cpp | 137 | 标签图元实现 |
| Gui/main.cpp | 10 | 程序入口 |

### B. 依赖关系图
```
SimulationManager
    └─> UavModel
         └─> TrajectoryGenerator

MainWindow
    ├─> SimulationManager (数据源)
    ├─> SimScene (场景)
    │    └─> SimView (视图)
    ├─> UavItem (无人机图元)
    ├─> PathItem (航迹图元)
    └─> UavLabelItem (标签图元)
```

### C. 关键性能指标
| 指标 | 当前值 | 建议值 |
|------|-------|-------|
| 帧率 | 1 FPS (1000ms间隔) | 可配置 (30-60 FPS) |
| UAV数量 | 5 (硬编码) | 可配置 (100+) |
| 内存占用 | 未测量 | < 100MB (100 UAVs) |
| 启动时间 | 未测量 | < 1s |

---

**报告结束**

如有任何问题或需要进一步的技术支持，请联系开发团队。
